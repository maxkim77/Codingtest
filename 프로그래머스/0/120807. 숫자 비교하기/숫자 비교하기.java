class Solution {
    public int solution(int num1, int num2) {
        if (num1 == num2) // 조건문에서 괄호가 필요합니다.
            return 1; 
        else // else 다음에는 콜론(:)이 아니라 중괄호({})를 사용해야 합니다.
            return -1;
    }
}
